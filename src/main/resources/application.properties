spring.datasource.url= jdbc:mysql://remotemysql.com:3306/FTepCNx6EZ?allowPublicKeyRetrieval=true&autoReconnect=true&useSSL=false
spring.datasource.username=FTepCNx6EZ
spring.datasource.password=qinulsZ3Fz


jwt.secret=rashcomputers-app-secrete-hashed

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL8Dialect

spring.datasource.maxActive=10
spring.datasource.maxIdle=5
spring.datasource.minIdle=2
spring.datasource.initialSize=5
spring.datasource.removeAbandoned=true
spring.jpa.hibernate.ddl-auto=create
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.model-converters.pageable-converter.enabled=true

spring.main.allow-bean-definition-overriding=true


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database

#spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
##Loggin in spring boot
logging.file.name=application.log

#Disabling the spring boot banner
logging.main.banner-mode=off

#changing the color of the output in console
spring.output.ansi.enabled=ALWAYS

#Enabling Debug logs
debug=false
#Enabling Trace logs
trace=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
## MULTIPART (MultipartProperties)
# Enable multipart uploads
multipart.enabled=true
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB
# All files uploaded through the REST API will be stored in this directory

#upload.profile=src/uploads
#upload.document=src/cloudStore

spring.mail.host=smtp.gmail.com
spring.mail.port=587

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true

